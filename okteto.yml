name: getting-started
# dependencies:
#   infrastructure:
#     repository: https://github.com/burstsms/infrastructure-okteto-shared
#     branch: KJU-1522_use_shared_infra
#     variables:
#       CONFIG: fullstack
#     wait: true
#     timeout: 15m
deploy:
  # Use our custom runner so we have access to extra utilities like pt migration
  image: "ghcr.io/burstsms/pipeline-runner:main"
  commands:
    - find /okteto
    - okteto deploy --file subdir/okteto-dep.yml --name getting-started-2 --wait --dependencies
