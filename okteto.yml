name: getting-started
deploy:
  remote: true
  image: okteto/pipeline-runner:1.31.0
  context: .
  commands:
    - find .
    - okteto deploy --file subdir/okteto-dep.yml --name getting-started-2 --wait --dependencies
